# Product

## Register

product

## Users

主要用户是 UPS 设备拥有者、实验室操作人员、基础设施维护者和固件开发者。他们会在浏览器里同时监督一台或多台 Mains Aegis 设备，常见场景包括检查设备健康、比较多台设备状态、诊断告警、验证固件行为，以及确认设备 API 是否正常响应。

这些用户通常不是在“浏览一个漂亮仪表盘”，而是在排查或确认真实硬件状态。界面需要让他们快速判断哪些设备正常、哪些设备离线、哪些设备处于后备供电、哪些设备需要立即处理。

## Product Purpose

Web 管理界面是一个面向多设备 UPS 的只读运维控制台。它的第一目标是让 fleet 健康状态一眼可见，然后让用户进入单设备详情页查看电源、电池、热状态、设备信息和 API 调试数据。

首版成功标准是：用户可以快速回答“哪台设备需要关注”、“哪台设备离线”、“每台 UPS 当前处于什么运行状态”、“设备是否正在使用电池后备供电”、“API 连接是否健康”。首版不提供会改变 UPS 状态的控制操作，避免误触风险。

## Brand Personality

冷静、精确、可靠。

界面应该像工程仪器，而不是营销页面。它需要足够清晰，让非专业拥有者也能理解当前风险；也要足够具体，让维护者和固件开发者能够继续深入排查。故障状态要明确，但不能让整个界面显得慌乱。

## Anti-references

不要把总览页做成原始遥测数据倾倒。高层卡片不应暴露 `OUT A/B`、charger、API 等硬件或调试细节，除非用户已经进入详情页或 API 调试页。

不要做营销式 hero、装饰性 dashboard、深蓝 observability 套路、过度戏剧化的报警视觉，也不要用会误导语义的图标。例如 Power 显示 `Battery` 时，图标应表达“UPS 正以后备电源模式工作”或“电池正在供电”，不能像“正在充电”，也不能像“电池已经空了”。

## Design Principles

1. 优先表达真实运行状态，而不是堆叠装饰性指标。
2. 总览页使用用户能理解的摘要语言，技术标签留给详情页和 API 调试页。
3. 多设备比较是默认工作流，总览页应使用卡片和网格帮助用户扫描设备状态。
4. Critical、Warning、Offline、Stale 等状态必须视觉上可区分，但不能让界面整体失控或制造噪音。
5. 首版所有交互保持只读，把设备管理、API 调试和 owner-facing 健康视图清晰分层。
6. 所有可测试示例应复用正式前端和正式路由，通过 mock 数据 seed 提供稳定、可交互、可直接访问的演示路径。

## Accessibility & Inclusion

目标满足 WCAG AA 文本和状态对比度。严重程度、连接状态和运行模式不能只依赖颜色表达，必须同时有文字、图标或结构提示。

界面需要尊重 reduced motion 偏好，移动端触控目标要足够舒适。长 hostname、位置、故障消息和 API 错误都必须能自然换行，不能重叠或撑破布局。
