# tps55288 (tool-local vendor copy)

This directory is a minimal vendor snapshot used by `tools/bq40-comm-tool` firmware.

Kept in this copy:
- `src/` driver implementation
- `Cargo.toml`
- licenses (`LICENSE-APACHE`, `LICENSE-MIT`)

Dropped intentionally:
- upstream CI/dev files
- examples/tests/docs assets

For full upstream project context, see the repository URL declared in `Cargo.toml`.
