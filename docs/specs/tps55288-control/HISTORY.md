# TPS55288 双路输出控制 演进历史

> 这里记录影响当前规范理解的关键演进；当前有效合同仍以 `./SPEC.md` 为准。

## Decision Trace

- 2026-01-22: 初始化计划与契约骨架
- 2026-01-23: 纳入 INA3221 初始化与 `500ms` 遥测日志契约
- 2026-01-24: 实现默认 profile（`out_a=5V/1A`）、遥测输出与 `I2C1_INT` 最小故障观测；补齐 bring-up README
- 2026-01-25: bring-up 增强：解除 CDC fault mask、补全 fault 寄存器打印（含 `VOUT_SR/MODE` 关键位）；调大 `OCP_DELAY` 并放慢 `VOUT` 斜率用于排查启动瞬态误触发；修正 telemetry 字段顺序以符合 CLI 契约
- 2026-01-25: 调试配置：默认启用通道切换为 `out_b`
- 2026-01-25: 调试验证：默认启用切回 `out_a`；默认输出电压临时改为 `9V` 用于稳定性检查
- 2026-01-25: 调试验证：默认输出电压临时改为 `12V` 用于稳定性检查
- 2026-01-25: 调试验证：默认输出电压临时改为 `15V` 用于稳定性检查
- 2026-01-25: 调试验证：双路同时启用（`out_a+out_b`），默认输出电压临时改为 `3.3V`，两路限流均为 `0.5A`
- 2026-01-25: 调试验证：双路 light-load 模式改为 `PFM`（不强制 FPWM）
- 2026-01-25: 调试验证：双路同时启用（`out_a+out_b`），默认输出电压改回 `5V`，两路限流均为 `1A`
- 2026-01-25: 调试验证：双路同时启用（`out_a+out_b`），默认输出电压临时改为 `19V`，两路限流均为 `3A`（PFM）
- 2026-01-25: INA3221 遥测排障：初始化时先触发 `RST` 并回读 `cfg/man_id/die_id`（确认为 `INA3221: 0x5449/0x3220`）；“空载电流异常”最终定位为 INA 输入 RC 串阻误贴 `10kΩ`（应为 `10Ω`），修正后读数恢复正常
- 2026-01-26: 调试配置：默认仅启用 `out_b`（单路输出；PFM；`19V/3A`）
- 2026-01-26: 调试配置：默认仅启用 `out_a`（单路输出；PFM；`19V/3A`）
- 2026-01-26: INA3221 遥测增强：追加 `dv_mv`（`vbus-vset`）用于快速对比；默认不输出 `VIN(CH3)` 遥测以满足 CLI 契约；补充 `VBUS` 偏差排查步骤到 `firmware/README.md`
- 2026-01-26: 调试配置：默认输出电压切换为 `13.8V`（`out_b` 单路）
- 2026-01-26: 调试配置：默认启用切换为 `out_a+out_b`，默认输出电压切换为 `15V`
- 2026-01-26: 调试配置：默认输出电压切换为 `19V`（`out_a+out_b`）
- 2026-01-26: 调试配置：默认限流切换为 `3.5A`（每路；`out_a+out_b`）
- 2026-08-09: 单路 TPS 启动通信失败改为通道级有限重试；健康同伴不再因缺失通道的重试或锁存被成对重配/关断，双路请求合同仍保持 `blocked`

## References

- `./SPEC.md`
- `./IMPLEMENTATION.md`
