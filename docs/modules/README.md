# 模块文档

本目录用于收拢功能模块说明，遵循“一模块一文件”的规则：

- 每个模块使用一个独立 Markdown 文件。
- 模块文档优先描述当前实现真相、模块边界、输入输出、状态机与验收口径。
- 若某个主题只是模块的硬件参考或历史计划，应继续放在 `docs/` 其它专题文档或 `docs/specs/` 中，不在这里重复铺开。

当前模块清单：

- 稳压输出模块：`docs/modules/regulated-output.md`
